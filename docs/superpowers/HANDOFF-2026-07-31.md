# Session handoff — redesign plan 4 (analytics + cleanup), mid-execution

**Written:** 2026-07-31 · **Branch:** `feat/redesign-04-analytics`

Read this top to bottom before doing anything. The redesign is nearly complete — plan 4 is the last plan, and **3 of its 8 tasks are done**. Plans 1, 2 and 3 are all **merged to `main`**; this branch (`feat/redesign-04-analytics`) has already been **rebased onto the post-#30 `main`**, so `main` is an ancestor and the eventual plan-4 PR will merge without a squash-rebase dance (unless `main` advances again first).

---

## 1. The big picture

A four-plan redesign applying a Claude Design handoff (the app scored 4.9/10) to this Finviz-style portfolio dashboard. Status:

| Plan | Scope | State |
|---|---|---|
| 1 | Foundation lib + heat map | **Merged to main** (squash, PR #28) |
| 2 | Dashboard screen | **Merged to main** (squash, PR #29) |
| 3 | Holdings screen + PositionSheet | **Merged to main** (squash, PR #30) |
| 4 | Analytics rewrite + legacy cleanup | **In progress on this branch** — 3/8 tasks done |

**Branch protection:** `main` requires **1 approving review**. You (the owner) must approve PRs on GitHub; the agent cannot self-approve. PR #30 is waiting on your approval to merge.

**Plan 4 file (authoritative):** `docs/superpowers/plans/2026-07-28-redesign-04-analytics-cleanup.md` — read it. It has complete TDD code for every task.

---

## 2. Standing rules (from user memory — do not violate)

- **Never** add `Co-Authored-By` or any Claude/AI mention to commits or PRs.
- Always branch before the first commit on new work (already on the plan-4 branch).
- When work is ready to merge, ask PR vs local merge; **default to opening a PR**.
- No "for recruiters"/LinkedIn framing in commits or comments.
- **Verify plan claims before building** — this plan series has had ~10 defects caught in execution (wrong prop names, contradictory tests, an always-$0 field, a squash-merge that hid a sector). Recompute; don't trust.

---

## 3. Exact state

```
2a8e053 feat(analytics): performance card with a zoomed y-axis   <- Plan 4 Task 3 ✅
1a55765 feat(analytics): P&L-by-sector card                       <- Plan 4 Task 2 ✅
cfb6bba feat(analytics): P&L by sector                            <- Plan 4 Task 1 ✅
a376439 docs: implementation plan 4 — analytics and legacy cleanup
```

- **Suite is GREEN:** 529 tests / 70 suites pass; `npm run lint` and `npx tsc --noEmit` clean.
- **Working tree clean** — only `?? AGENTS.md` and `?? ui-review/` (pre-existing, untracked, leave alone).
- Execution method: mostly **inline** (the `superpowers:subagent-driven-development` dispatches kept dying on a monthly spend-limit error, so the reliable path has been doing tasks inline with TDD + a mutation check + commit). Subagent dispatch is fine if the limit has reset; inline also works.

### What's done (plan 4)
- **Task 1** `src/lib/design/sector-pl.ts` — pure P&L-by-sector aggregation (largest |P&L| first, Other bucket, zero-basis guard). Mutation-tested.
- **Task 2** `src/components/SectorPLCard.tsx` — direct-labelled rows, glyph + colour.
- **Task 3** `src/components/PerformanceCard.tsx` — rd-styled recharts line, `yDomain` zoomed (not zero-anchored), honest empty state under 5 snapshots. Exports `yDomain`, `MIN_POINTS`.

---

## 4. NEXT UP — Task 4: restyle valuation cards + detail primitives to rd tokens

This is a **token swap, no behaviour change**. The guard is: existing tests stay green, `tsc`/`lint` clean. It is `in_progress` in the task tool.

**Files to edit (5):** `src/components/ui/Sheet.tsx`, `src/components/ui/DetailPanel.tsx`, `src/components/ChipDetail.tsx`, `src/components/AnalystSentimentCard.tsx`, `src/components/ValuationCard.tsx`.

**Why safe now:** after plan 3 moved the holdings detail to `PositionSheet`, these `Sheet`/`DetailPanel`/`ChipDetail` primitives are used ONLY by the two valuation cards (and the soon-deleted `EquityAllocationChart`). Restyling them touches nothing already migrated.

**The exact legacy classes present (already grepped):**
- `Sheet.tsx:62` — `bg-surface-card border-surface-border` → `bg-rd-card border-rd-border` (the `bento-card` at line 49 is only in a comment).
- `DetailPanel.tsx:52` — desktop panel `className="bento-card ..."` → `className="fixed z-50 rounded-xl border border-rd-border bg-rd-card p-5 text-sm shadow-[0_14px_34px_#00000099] ..."` (keep the positioning logic and `pointer-events-none`).
- `ChipDetail.tsx` — many: `text-foreground`→`text-rd-text`, `text-muted-foreground`→`text-rd-muted`, `text-positive`→`text-rd-gain`, `text-negative`→`text-rd-loss`, `bg-surface-border`→`bg-rd-inset`, drop `font-display`.
- `AnalystSentimentCard.tsx` (lines 15-19, 53) and `ValuationCard.tsx` (lines 15-18, 74) — same map: `bg-surface-card`→`bg-rd-card`, `border-surface-border`→`border-rd-border`, `bg-surface-border`→`bg-rd-inset`, `text-positive`→`text-rd-gain`, `text-negative`→`text-rd-loss`.

**Token map:** `bg-surface-card`/`bg-surface`→`bg-rd-card`; `border-surface-border`/`border-border`→`border-rd-border`; `text-foreground`→`text-rd-text`; `text-muted-foreground`→`text-rd-muted`; `text-positive`→`text-rd-gain`; `text-negative`→`text-rd-loss`; `bg-surface-border`→`bg-rd-inset`; drop `font-display`.

**Test churn is minimal:** the only style assertion in the three component tests is `ChipDetail.test.tsx:60` `toHaveStyle({ left: "50%" })` (a marker *position*, not a colour) — it will NOT break. No test asserts a legacy colour. So after the swap, run `npm test` and expect green with no test edits; if one breaks on a class name, update the expectation to the rd class (don't delete the assertion).

**Commit message:**
```
feat(analytics): restyle the valuation cards and detail primitives to rd tokens

The two surviving valuation cards and the DetailPanel/Sheet/ChipDetail
primitives they open move off the legacy oklch palette. No behaviour
change; the holdings detail already moved to PositionSheet in plan 3, so
these are their only remaining consumers.
```

---

## 5. Remaining tasks (5–8) — summary; full code is in the plan file

- **Task 5 — Rewrite `src/app/analytics/page.tsx`.** Onto `AppShell`/`TopBar`, one commit (spec §2). Four blocks: `PerformanceCard`, **reuse `AllocationStrip`** (takes `items`), `SectorPLCard`, and the valuation block (`AnalystSentimentCard` + `ValuationCard`). Drop `Navbar`, `SectorChart`, `EquityAllocationChart`, the holdings table, and the edit/confirm modals (editing lives on `/holdings` now). Keep import/add modals and the valuations fetch. Test: `src/__tests__/app/analytics-page.test.tsx` (stub the valuation/perf cards, mock `usePortfolioData`). **Reconcile every prop name against the sibling pages (`page.tsx`, `holdings/page.tsx`) and the real components — this is where the plan series repeatedly had wrong prop names.**
- **Task 6 — Delete legacy components.** `git rm` `Navbar`, `MobileMenu`, `HoldingsTable`, `SectorChart`, `EquityAllocationChart`, `PerformanceChart` + the test files that exist (`MobileMenu.test`, `HoldingsTable.test`, `SectorChart.test`, `PerformanceChart.test` — **`Navbar` and `EquityAllocationChart` have no test files**). Gate: `grep -rlnE "Navbar|MobileMenu|HoldingsTable|SectorChart|EquityAllocationChart|PerformanceChart" src/ | grep -v __tests__` must be empty first. `PositionsTable` ≠ `HoldingsTable` — don't delete the wrong one.
- **Task 7 — Delete the legacy palette (destructive).** In `globals.css` remove the oklch `:root` block (keep `--rd-*` + `--radius`), body gradients (→ `background-color: var(--rd-page)`), the `h1..h4 font-display` rule, and `.bento-card`/`.brand-mark`/`.delta-pill*`/`.app-divider*`. In `tailwind.config.ts` remove the legacy colour keys (keep the `rd` group, borderRadius, fontFamily, content). **Gate:** `grep -rnE "bento-card|brand-mark|delta-pill|app-divider|text-foreground|bg-surface|text-muted-foreground|text-positive|text-negative|oklch|font-display" src/ | grep -v __tests__` must be EMPTY before deleting. The build may not error on a dangling class — the grep is the gate, not the compiler.
- **Task 8 — Verify + smoke test.** Full green, then production-build smoke test at 1440px and 375px on `/demo`, `/demo/holdings`, `/demo/analytics`: no unstyled/black-on-black elements, no 375px overflow, clean console/server logs, and `grep -rl "oklch(" .next/static/css/` must find nothing (palette gone from output). **Serve the prod build with the dev server OFF** — building while `npm run dev` runs overwrites `.next` and serves an empty app (a lesson already learned).

---

## 6. When plan 4 is done

Use `superpowers:finishing-a-development-branch`. Per standing rules: **open a PR** for plan 4 → `main`. This branch was already rebased onto `main` after #30 merged (2026-07-31), so `main` is currently an ancestor — just push and open the PR. Only if `main` advances again before you open it will you need to re-sync:
```
git fetch origin main && git rebase origin/main   # (fast-forward-style; main is already an ancestor now)
```
Then `npm test` + `tsc`, push, open PR #31 → main. `main` requires 1 approving review — the owner approves on GitHub.

**Optional plan 5 (redesign is complete without it):** wire tile-tap / mover-row → `PositionSheet` (rework the hover `TreemapTooltip`), and the mobile heat-map top-10 + aggregate strip. Recorded in the plan-4 file's "Plan 5 inbox". `PositionSheet` is already surface-agnostic and `MobileHoldingsList` is already nav-aware, so no rework is needed for it.

## 7. Watch out for

- **Task 5 lands whole** (Analytics token migration in one commit) — a half-migrated Analytics at `#07090b` reads as a rendering bug.
- **Tasks 6 & 7 gate on greps, not the compiler.** A surviving legacy class becomes an unstyled element the moment its token is deleted, and the build may stay green. Run the grep; it must be empty.
- Keep `ResponsiveContainer` mocked in chart tests (jsdom reports zero size; recharts renders nothing otherwise). `PerformanceCard.test` already does this — copy the pattern for any new chart test.
- The valuation cards fetch their own valuation data via `/api/valuations` + `getDemoValuations` — don't rip that out during the restyle or rewrite.
- Subagent dispatches were hitting a **monthly spend limit**. If they still are, work inline (write test → confirm it fails for the right reason → implement → mutation-check one or two guards → `npm test`/`lint`/`tsc` → commit).
